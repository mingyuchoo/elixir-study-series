# Phoenix Configuration
SECRET_KEY_BASE=your_secret_key_base_here
PHX_HOST=localhost
PORT=4000
PHX_SERVER=true

# Database Configuration
DATABASE_PATH=/app/data/elixir_blog.db
POOL_SIZE=5

# DNS Cluster (optional, for distributed deployment)
DNS_CLUSTER_QUERY=

# Generate SECRET_KEY_BASE with:
# mix phx.gen.secret
